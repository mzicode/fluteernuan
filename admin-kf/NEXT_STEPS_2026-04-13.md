# 官方客服后台下一步记录

更新时间：2026-04-13

## 当前已完成

### 登录与安全
- 真实登录接入完成
- Token 持久化与刷新恢复完成
- 全局未授权跳回登录页已接入
- 修改密码后旧 Token 失效已接入
- 退出登录后旧 Token 失效已接入
- `session_version` 基础机制已接入到：
  - 官方客服后台登录
  - 普通用户登录
  - 普通用户注册后自动登录
  - QR 登录确认
  - Refresh Token

### 客服后台页面
- 工作台统计增强完成
- 最近新增用户摘要完成
- 邀请码复制 / 注册链接复制完成
- 我的用户列表完成
- 欢迎语读取 / 保存 / 字数统计 / 未保存提醒完成
- 欢迎语预设模板完成
- 欢迎语恢复系统模板完成
- 手机号绑定流程完成
- 个人中心修改密码完成

### 体验增强
- 页面空态 / 错误态 / 重试已完成
- 工作台跳转到我的用户页已接入
- 我的用户页支持 UUID 复制
- 我的用户页支持根据路由参数高亮目标用户

---

## 明天建议继续做

### P1：把工作台到用户页联动补完整
1. 点击工作台最近新增用户时，携带 `highlight=<uuid>` 和 `keyword=<uuid>`
2. `invitees` 页面进入后自动滚动到高亮用户
3. 如果命中高亮用户，增加一次短暂高亮动画，便于定位

### P2：整理后端会话版本逻辑
1. 把 `session_version` 的写入逻辑抽成公共方法
2. 把 `logout_after` 与 `session_version` 的判断收口，避免各 handler 重复实现
3. 检查 refresh token、二维码登录、普通登录三条链路是否完全一致

### P3：欢迎语页继续产品化
1. 显示“当前选中的模板名称”
2. 恢复系统模板前增加确认提示
3. 保存成功后显示后端返回时间或统一的最后保存时间字段

### P4：宝塔部署后的联调清单
1. 检查 `VITE_API_BASE_URL` 是否正确
2. 检查反向代理是否已把 `/api/v1` 转发到 Go 服务
3. 检查短信配置、Redis、MySQL、MongoDB 是否在生产环境可用
4. 用真实官方客服账号跑一遍完整登录流程

---

## 明天开工顺序建议
1. 先补工作台到用户页精准联动
2. 再收口 `session_version` 公共逻辑
3. 最后补欢迎语页剩余体验细节
