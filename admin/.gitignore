node_modules
.DS_Store
dist
dist-ssr
*.local
.cursorrules

# Auto-generated files
# These declaration files are generated by Vite plugins, but typecheck runs
# before Vite build in CI/local builds, so they must stay available.
.auto-import.json
